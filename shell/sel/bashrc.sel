#!/usr/bash
unset RC_SEL
# export RC_SEL=bashrc.mac
# export RC_SEL=bashrc.cuda

if [ -z ${RC_SEL+x} ]; then
    echo RC_SEL not set!
    echo "Press enter to continue"
    read 
    vi ~/.bashrc.sel
fi
alias ep="vi ${ENV_ROOT}/shell/sel/${RC_SEL}"

source ${ENV_ROOT}/shell/sel/${RC_SEL}
