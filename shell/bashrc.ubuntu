#!/usr/bash
export ENV_ROOT="$( cd "$( dirname "${BASH_SOURCE[0]}" )"/.. && pwd )"
source $ENV_ROOT/shell/common/bashrc

alias sb='source ~/.bashrc'
alias eb='vi $ENV_ROOT/shell'

alias ls='ls --color=auto'

